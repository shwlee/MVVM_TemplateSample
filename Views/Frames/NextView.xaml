<UserControl x:Class="Views.Frames.NextView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Views.Frames"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid Background="Gray">
        <StackPanel>
            <Button Width="50" Height="20" Content="GoMain" Command="{Binding GoMainCommand}" />
            <TextBlock FontSize="500" Text="Next" Foreground="AliceBlue" VerticalAlignment="Center" HorizontalAlignment="Center"/>
        </StackPanel>
            
    </Grid>
</UserControl>
